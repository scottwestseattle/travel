// drop all tables and migrate all
php artisan migrate:reset

// add field to table
php artisan make:migration add_settings_to_users --table=users

// migrate / rollback
php artisan migrate
php artisan migrate:rollback

// rollback the last migration only:
php artisan migrate:rollback --step=1


// create Migration, Model and Controller
php artisan make:model Tag -m -c



TODO:
- Add tour attributes
- Add content / maps
- Refine visits to add counts
- Add private/published/reviewed flag for tours
- add site id to every thing
- add location tags

DONE:
x Show slider location text from db
x Admin Dashboard

~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

Primary Colors:

Header Orange:
$color = '#FF6900'; // orange header

Header Blue:
$color = '#4993FD'; // blue header

Footer Green:
$color = '#5CB85C'; // green

