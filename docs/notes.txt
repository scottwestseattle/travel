
// drop all tables and migrate all
php artisan migrate:reset

// add field to table
php artisan make:migration add_settings_to_users --table=users

// migrate / rollback
php artisan migrate
php artisan migrate:rollback

// rollback the last migration only, step = how many migrations to rollback
php artisan migrate:rollback --step=1

// create Migration, Model and Controller
php artisan make:model Tag -m -c

~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

Primary Colors:

Header Orange:
$color = '#FF6900'; // orange header

Header Blue:
$color = '#4993FD'; // blue header

Footer Green:
$color = '#5CB85C'; // green

~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

DB Changes

ALTER TABLE `entries` ADD `display_date` DATE NULL AFTER `deleted_flag`;

ALTER TABLE `events` ADD `ip_address` VARCHAR(50) NULL AFTER `user_id`;

ALTER TABLE `entries` ADD UNIQUE(`permalink`);

ALTER TABLE `photos` ADD `site_id` INT NOT NULL AFTER `id`;

ALTER TABLE `sites` ADD `instagram_link` VARCHAR(100) NULL AFTER `main_section_subtext`, ADD `facebook_link` VARCHAR(100) NULL AFTER `instagram_link`, ADD `twitter_link` VARCHAR(100) NULL AFTER `facebook_link`, ADD `other_link` VARCHAR(100) NULL AFTER `twitter_link`;

UPDATE photos SET site_id = 1 WHERE 1=1;

UPDATE activities SET site_id = 1 WHERE 1=1;






