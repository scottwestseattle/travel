
// drop all tables and migrate all
php artisan migrate:reset

// add field to table
php artisan make:migration add_settings_to_users --table=users

// migrate / rollback
php artisan migrate
php artisan migrate:rollback

// rollback the last migration only, step = how many migrations to rollback
php artisan migrate:rollback --step=1

// create Migration, Model and Controller
php artisan make:model Tag -m -c

~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

Primary Colors:

Header Orange:
$color = '#FF6900'; // orange header

Header Blue:
$color = '#4993FD'; // blue header

Footer Green:
$color = '#5CB85C'; // green

~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

DB Changes

ALTER TABLE `entries` ADD `display_date` DATE NULL AFTER `deleted_flag`;

ALTER TABLE `events` ADD `ip_address` VARCHAR(50) NULL AFTER `user_id`;

ALTER TABLE `entries` ADD UNIQUE(`permalink`);

ALTER TABLE `photos` ADD `site_id` INT NOT NULL AFTER `id`;

ALTER TABLE `sites` ADD `instagram_link` VARCHAR(100) NULL AFTER `main_section_subtext`, ADD `facebook_link` VARCHAR(100) NULL AFTER `instagram_link`, ADD `twitter_link` VARCHAR(100) NULL AFTER `facebook_link`, ADD `other_link` VARCHAR(100) NULL AFTER `twitter_link`;

UPDATE photos SET site_id = 1 WHERE 1=1;

UPDATE activities SET site_id = 1 WHERE 1=1;

ALTER TABLE `entries` drop index `permalink`;

ALTER TABLE `events` CHANGE `description` `description` TEXT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci NULL DEFAULT NULL;

6/10

ALTER TABLE `sites` ADD `current_location_title` VARCHAR(100) NULL AFTER `sections-show-articles`;
ALTER TABLE `sites` ADD `current_location` VARCHAR(100) NULL AFTER `current_location_title`;
ALTER TABLE `sites` ADD `previous_location_title` VARCHAR(100) NULL AFTER `current_location_photo`;

6/14

ALTER TABLE `entries` ADD `order_flag` SMALLINT NULL AFTER `type_flag`;
ALTER TABLE `sites` DROP `sections-show-blogs`, DROP `sections-show-tours`, DROP `sections-show-articles`, DROP `current_location_title`, DROP `main_section_text`, DROP `main_section_subtext`;

6/15

ALTER TABLE `transactions` ADD `transfer_id` INT NULL COMMENT 'id of the first transfer transaction' AFTER `transfer_account_id`;



